export interface menu {
    title: string;
    id: number;
    link: string;
    isActive: boolean;
}

export interface cardDetail {
    numberOfCardsInRow: number;
    height: number;
    cardHeaders: string[];
}

